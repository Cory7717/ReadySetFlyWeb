import { useQuery } from "@tanstack/react-query"; import { Link } from "wouter"; import { ArrowLeft, Bookmark } from "lucide-react"; import { BriefingCard } from "@/components/aviation-briefings/BriefingCard"; import type { AviationBriefing } from "@/components/aviation-briefings/types";
export default function SavedAviationBriefingsPage(){const {data,isLoading}=useQuery<{briefings:Array<{briefing:AviationBriefing;savedAt:string}>}>({queryKey:["/api/aviation-briefings/saved/me"],queryFn:async()=>{const r=await fetch("/api/aviation-briefings/saved/me",{credentials:"include"});if(!r.ok)throw new Error("Sign in required");return r.json();}});return <main className="min-h-screen bg-[#07101c] px-5 py-14 text-white"><div className="mx-auto max-w-7xl"><Link href="/briefings" className="inline-flex items-center text-[#8dbbfa]"><ArrowLeft className="mr-2 h-4 w-4"/>Ready Set Fly | Briefings</Link><h1 className="mt-6 flex items-center text-4xl font-black"><Bookmark className="mr-3 h-8 w-8"/>Saved Briefings</h1>{isLoading?<p className="mt-10">Loading…</p>:!data?.briefings.length?<p className="mt-10 rounded-xl border border-[#526d94]/40 bg-[#0c1624] p-10 text-center text-[#aebdce]">You have not saved any briefings yet.</p>:<div className="mt-8 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{data.briefings.map(x=><BriefingCard key={x.briefing.id} briefing={x.briefing}/>)}</div>}</div></main>}
